variables:
    MAINTENANCE_MODE: 1

stages:
    - test

default:
    image: golang:1.18.9-buster

    before_script:
        - echo ${CI_PROJECT_DIR}

unit_tests:
    stage: test
    # dependencies:
    script:
        - make build
        - make test
