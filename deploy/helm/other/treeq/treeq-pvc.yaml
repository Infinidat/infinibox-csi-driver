apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ibox-tree-pvc-demo
  namespace: infi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-tree-storageclass-demo
 

