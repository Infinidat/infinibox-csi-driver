apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sc-replica
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: sc-replica
  #volumeName: <<pv name>> #need to uncomment if want to existing pv
