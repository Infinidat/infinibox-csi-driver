apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvme-pvc-rox-readers
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-nvme-rox-readers
  volumeName: rox-pv-from-existing
