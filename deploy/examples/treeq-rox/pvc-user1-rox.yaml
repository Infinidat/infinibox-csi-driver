apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user1-rox-pvc
  namespace: infinidat-csi
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-treeq-admin
  volumeName: user1-pv
