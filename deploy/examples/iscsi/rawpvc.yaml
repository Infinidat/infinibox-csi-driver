apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: block-pvc
  namespace: infinidat-csi
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 10Gi
  storageClassName: ibox-iscsi-storageclass-demo