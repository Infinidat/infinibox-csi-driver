apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc-fsg-v4
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-nfs-fsg-v4
  # volumeName: <<pv name>> #need to uncomment if want to use an existing pv
