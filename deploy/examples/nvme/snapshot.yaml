apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: nvme-snapshot
  namespace: $_E2E_NAMESPACE
spec:
  volumeSnapshotClassName: ibox-snapshotclass
  source:
    persistentVolumeClaimName: nvme-pvc

