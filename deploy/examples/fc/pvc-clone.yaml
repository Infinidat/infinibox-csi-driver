apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: fc-pvc-clone
    namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: $TEST_NAME
  resources:
    requests:
      storage: 1Gi
  dataSource:
    kind: PersistentVolumeClaim
    name: $TEST_NAME
