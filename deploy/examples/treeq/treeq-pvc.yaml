apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ibox-tree-pvc-demo15
  namespace: infi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: ibox-tree-storageclass-demo
 

