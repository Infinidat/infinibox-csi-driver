apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fc-pvc-from-existing
  namespace: infinidat-csi
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-fc
  volumeName: jeff-volume
