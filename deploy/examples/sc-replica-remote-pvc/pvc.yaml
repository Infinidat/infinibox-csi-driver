apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sc-replica-remote-pvc
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: sc-replica-remote-pvc
  #volumeName: <<pv name>> #need to uncomment if want to existing pv
