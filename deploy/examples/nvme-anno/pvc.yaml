apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvme-pvc-anno
  namespace: $_E2E_NAMESPACE
  annotations:
    infinidat.com/network_space: $_E2E_NVME_NETWORK_SPACE
    infinidat.com/pool_name: $_E2E_POOL
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-nvme-anno
  #volumeName: <<pv name>> #need to uncomment if want to existing pv
