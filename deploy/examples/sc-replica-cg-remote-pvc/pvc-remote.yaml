apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sc-replica-cg-remote-pvc$_E2E_IBOXREPLICA_REMOTE_PVC_NAME_SUFFIX
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: sc-replica-cg-remote-pvc
  #volumeName: <<pv name>> #need to uncomment if want to existing pv
