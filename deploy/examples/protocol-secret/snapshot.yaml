apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: ps-snapshot
  namespace: $_E2E_NAMESPACE
spec:
  volumeSnapshotClassName: ibox-snapshotclass
  source:
    persistentVolumeClaimName: ps-pvc

