apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ps-pvc
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ibox-ps
  #volumeName: <<pv name>> #need to uncomment if want to existing pv
