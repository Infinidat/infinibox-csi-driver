apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user1-pvc
  namespace: $_E2E_NAMESPACE
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: $TEST_NAME
  volumeName: user1-pv
